import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("System Audio Recorder")
            .font(.title)
            .padding()
            .frame(minWidth: 400, minHeight: 200)
    }
}

#Preview {
    ContentView()
}
